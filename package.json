{
  "name": "mail-analyzer",
  "version": "1.0.0",
  "description": "Lambda function to analyze email responses to job applications",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "get-token": "node get-refresh-token.js",
    "test-local": "node test-local.js"
  },
  "keywords": [
    "lambda",
    "email",
    "imap",
    "outlook"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0",
    "node-fetch": "^2.7.0",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "dotenv": "^16.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
